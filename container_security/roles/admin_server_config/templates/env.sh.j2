#!/bin/bash

# Set inventory to localhost only to avoid recursion on inventory lookup
credentials=$(ansible-vault decrypt --output - aws_credentials_vault.yml)

export AWS_ACCESS_KEY_ID=$(echo $credentials | cut -d\  -f2)
export AWS_SECRET_ACCESS_KEY=$(echo $credentials | cut -d\  -f4)
